---
title: Schedule Metrics (Location)
description: The Data Dictionary entry for the Schedule Metrics (Location) entity
slug: /data-dictionary/schedule-metrics-location
---

import entities from "@site/src/data/entities.json";

# Schedule Metrics (Location)

{/* prettier-ignore */}
{Array.isArray(entities.scheduleMetricsLocation.description) ? (
  entities.scheduleMetricsLocation.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleMetricsLocation.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleMetricsLocation.supportedTypes} />

## Data Granularity

{entities.scheduleMetricsLocation.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleMetricsLocation.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleMetricsLocation.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleMetricsLocation.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleMetricsLocation.relationship}
  simple={true}
/>

{entities.scheduleMetricsLocation.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleMetricsLocation.additionalInfo) ? (
      entities.scheduleMetricsLocation.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleMetricsLocation.additionalInfo}</p>
    )}

  </>
)}
