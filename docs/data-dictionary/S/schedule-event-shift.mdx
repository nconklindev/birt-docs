---
title: Schedule Event Shift
description: The Data Dictionary entry for the Schedule Event Shift entity
slug: /data-dictionary/schedule-event-shift
---

import entities from "@site/src/data/entities.json";

# Schedule Event Shift

{/* prettier-ignore */}
{Array.isArray(entities.scheduleEventShift.description) ? (
  entities.scheduleEventShift.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleEventShift.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleEventShift.supportedTypes} />

## Data Granularity

{entities.scheduleEventShift.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleEventShift.columns} />

## Related Entities (Employee)

<RelatedEntitiesList relatedEntities={entities.scheduleEventShift.relatedEntities.employee} />

## Related Entities (Business Structure)

<RelatedEntitiesList relatedEntities={entities.scheduleEventShift.relatedEntities.businessStructure} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleEventShift.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleEventShift.relationship}
  simple={true}
/>

{entities.scheduleEventShift.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleEventShift.additionalInfo) ? (
      entities.scheduleEventShift.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleEventShift.additionalInfo}</p>
    )}

  </>
)}
