---
title: Schedule Zone
description: The Data Dictionary entry for the Schedule Zone entity
slug: /data-dictionary/schedule-zone
---

import entities from "@site/src/data/entities.json";

# Schedule Zone

{/* prettier-ignore */}
{Array.isArray(entities.scheduleZone.description) ? (
  entities.scheduleZone.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleZone.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleZone.supportedTypes} />

## Data Granularity

{entities.scheduleZone.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleZone.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleZone.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleZone.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleZone.relationship}
  simple={true}
/>

{entities.scheduleZone.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleZone.additionalInfo) ? (
      entities.scheduleZone.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleZone.additionalInfo}</p>
    )}

  </>
)}
