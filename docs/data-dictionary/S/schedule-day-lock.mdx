---
title: Schedule Day Lock
description: The Data Dictionary entry for the Schedule Day Lock entity
slug: /data-dictionary/schedule-day-lock
---

import entities from "@site/src/data/entities.json";

# Schedule Day Lock

{/* prettier-ignore */}
{Array.isArray(entities.scheduleDayLock.description) ? (
  entities.scheduleDayLock.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleDayLock.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleDayLock.supportedTypes} />

## Data Granularity

{entities.scheduleDayLock.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleDayLock.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleDayLock.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleDayLock.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleDayLock.relationship}
  simple={true}
/>

{entities.scheduleDayLock.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleDayLock.additionalInfo) ? (
      entities.scheduleDayLock.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleDayLock.additionalInfo}</p>
    )}

  </>
)}
