---
title: Scheduled Totals
description: The Data Dictionary entry for the Scheduled Totals entity
slug: /data-dictionary/scheduled-totals
---

import entities from "@site/src/data/entities.json";

# Scheduled Totals

{/* prettier-ignore */}
{Array.isArray(entities.scheduledTotals.description) ? (
  entities.scheduledTotals.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduledTotals.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduledTotals.supportedTypes} />

## Data Granularity

{entities.scheduledTotals.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduledTotals.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduledTotals.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduledTotals.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduledTotals.relationship}
  simple={true}
/>

{entities.scheduledTotals.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduledTotals.additionalInfo) ? (
      entities.scheduledTotals.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduledTotals.additionalInfo}</p>
    )}

  </>
)}
