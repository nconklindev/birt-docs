---
title: Schedule Metrics by Day (Location)
description: The Data Dictionary entry for the Schedule Metrics by Day (Location) entity
slug: /data-dictionary/schedule-metrics-by-day-location
---

import entities from "@site/src/data/entities.json";

# Schedule Metrics by Day (Location)

{/* prettier-ignore */}
{Array.isArray(entities.scheduleMetricsByDayLocation.description) ? (
  entities.scheduleMetricsByDayLocation.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleMetricsByDayLocation.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleMetricsByDayLocation.supportedTypes} />

## Data Granularity

{entities.scheduleMetricsByDayLocation.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleMetricsByDayLocation.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleMetricsByDayLocation.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleMetricsByDayLocation.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleMetricsByDayLocation.relationship}
  simple={true}
/>

{entities.scheduleMetricsByDayLocation.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleMetricsByDayLocation.additionalInfo) ? (
      entities.scheduleMetricsByDayLocation.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleMetricsByDayLocation.additionalInfo}</p>
    )}

  </>
)}
