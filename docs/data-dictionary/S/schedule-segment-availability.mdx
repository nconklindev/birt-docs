---
title: Schedule Segment Availability
description: The Data Dictionary entry for the Schedule Segment Availability entity
slug: /data-dictionary/schedule-segment-availability
---

import entities from "@site/src/data/entities.json";

# Schedule Segment Availability

{/* prettier-ignore */}
{Array.isArray(entities.scheduleSegmentAvailability.description) ? (
  entities.scheduleSegmentAvailability.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleSegmentAvailability.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleSegmentAvailability.supportedTypes} />

## Data Granularity

{entities.scheduleSegmentAvailability.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleSegmentAvailability.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleSegmentAvailability.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleSegmentAvailability.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleSegmentAvailability.relationship}
  simple={true}
/>

{entities.scheduleSegmentAvailability.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleSegmentAvailability.additionalInfo) ? (
      entities.scheduleSegmentAvailability.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleSegmentAvailability.additionalInfo}</p>
    )}

  </>
)}
