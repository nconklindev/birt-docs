---
title: Schedule Group Assignment
description: The Data Dictionary entry for the Schedule Group Assignment entity
slug: /data-dictionary/schedule-group-assignment
---

import entities from "@site/src/data/entities.json";

# Schedule Group Assignment

{/* prettier-ignore */}
{Array.isArray(entities.scheduleGroupAssignment.description) ? (
  entities.scheduleGroupAssignment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleGroupAssignment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleGroupAssignment.supportedTypes} />

## Data Granularity

{entities.scheduleGroupAssignment.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleGroupAssignment.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleGroupAssignment.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleGroupAssignment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleGroupAssignment.relationship}
  simple={true}
/>

{entities.scheduleGroupAssignment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleGroupAssignment.additionalInfo) ? (
      entities.scheduleGroupAssignment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleGroupAssignment.additionalInfo}</p>
    )}

  </>
)}
