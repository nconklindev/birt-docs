---
title: Schedule Metrics by Span (Job)
description: The Data Dictionary entry for the Schedule Metrics by Span (Job) entity
slug: /data-dictionary/schedule-metrics-by-span-job
---

import entities from "@site/src/data/entities.json";

# Schedule Metrics by Span (Job)

{/* prettier-ignore */}
{Array.isArray(entities.scheduleMetricsBySpanJob.description) ? (
  entities.scheduleMetricsBySpanJob.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleMetricsBySpanJob.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleMetricsBySpanJob.supportedTypes} />

## Data Granularity

{entities.scheduleMetricsBySpanJob.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleMetricsBySpanJob.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleMetricsBySpanJob.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleMetricsBySpanJob.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleMetricsBySpanJob.relationship}
  simple={true}
/>

{entities.scheduleMetricsBySpanJob.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleMetricsBySpanJob.additionalInfo) ? (
      entities.scheduleMetricsBySpanJob.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleMetricsBySpanJob.additionalInfo}</p>
    )}

  </>
)}
