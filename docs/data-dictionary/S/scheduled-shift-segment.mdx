---
title: Scheduled Shift Segment
description: The Data Dictionary entry for the Scheduled Shift Segment entity
slug: /data-dictionary/scheduled-shift-segment
---

import entities from "@site/src/data/entities.json";

# Scheduled Shift Segment

{/* prettier-ignore */}
{Array.isArray(entities.scheduledShiftSegment.description) ? (
  entities.scheduledShiftSegment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduledShiftSegment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduledShiftSegment.supportedTypes} />

## Data Granularity

{entities.scheduledShiftSegment.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduledShiftSegment.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduledShiftSegment.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduledShiftSegment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduledShiftSegment.relationship}
  simple={true}
/>

{entities.scheduledShiftSegment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduledShiftSegment.additionalInfo) ? (
      entities.scheduledShiftSegment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduledShiftSegment.additionalInfo}</p>
    )}

  </>
)}
