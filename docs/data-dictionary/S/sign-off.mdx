---
title: Sign Off
description: The Data Dictionary entry for the Sign Off entity
slug: /data-dictionary/sign-off
---

import entities from "@site/src/data/entities.json";

# Sign Off

{/* prettier-ignore */}
{Array.isArray(entities.signOff.description) ? (
  entities.signOff.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.signOff.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.signOff.supportedTypes} />

## Data Granularity

{entities.signOff.dataGranularity}

## Columns

<ColumnsTable columns={entities.signOff.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.signOff.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.signOff.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.signOff.relationship}
  simple={true}
/>

{entities.signOff.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.signOff.additionalInfo) ? (
      entities.signOff.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.signOff.additionalInfo}</p>
    )}

  </>
)}
