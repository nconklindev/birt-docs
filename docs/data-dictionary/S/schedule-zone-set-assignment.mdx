---
title: Schedule Zone Set Assignment
description: The Data Dictionary entry for the Schedule Zone Set Assignment entity
slug: /data-dictionary/schedule-zone-set-assignment
---

import entities from "@site/src/data/entities.json";

# Schedule Zone Set Assignment

{/* prettier-ignore */}
{Array.isArray(entities.scheduleZoneSetAssignment.description) ? (
  entities.scheduleZoneSetAssignment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleZoneSetAssignment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleZoneSetAssignment.supportedTypes} />

## Data Granularity

{entities.scheduleZoneSetAssignment.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleZoneSetAssignment.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleZoneSetAssignment.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleZoneSetAssignment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleZoneSetAssignment.relationship}
  simple={true}
/>

{entities.scheduleZoneSetAssignment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleZoneSetAssignment.additionalInfo) ? (
      entities.scheduleZoneSetAssignment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleZoneSetAssignment.additionalInfo}</p>
    )}

  </>
)}
