---
title: Schedule Events Pay Code Edit
description: The Data Dictionary entry for the Schedule Events Pay Code Edit entity
slug: /data-dictionary/schedule-events-paycode-edit
---

import entities from "@site/src/data/entities.json";

# Schedule Events Pay Code Edit

{/* prettier-ignore */}
{Array.isArray(entities.scheduleEventsPaycodeEdit.description) ? (
  entities.scheduleEventsPaycodeEdit.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleEventsPaycodeEdit.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleEventsPaycodeEdit.supportedTypes} />

## Data Granularity

{entities.scheduleEventsPaycodeEdit.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleEventsPaycodeEdit.columns} />

## Related Entities (Employee)

<RelatedEntitiesList relatedEntities={entities.scheduleEventsPaycodeEdit.relatedEntities.employee} />

## Related Entities (Business Structure)

<RelatedEntitiesList relatedEntities={entities.scheduleEventsPaycodeEdit.relatedEntities.businessStructure} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleEventsPaycodeEdit.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleEventsPaycodeEdit.relationship}
  simple={true}
/>

{entities.scheduleEventsPaycodeEdit.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleEventsPaycodeEdit.additionalInfo) ? (
      entities.scheduleEventsPaycodeEdit.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleEventsPaycodeEdit.additionalInfo}</p>
    )}

  </>
)}
