---
title: Schedule Summary
description: The Data Dictionary entry for the Schedule Summary entity
slug: /data-dictionary/schedule-summary
---

import entities from "@site/src/data/entities.json";

# Schedule Summary

{/* prettier-ignore */}
{Array.isArray(entities.scheduleSummary.description) ? (
  entities.scheduleSummary.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleSummary.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleSummary.supportedTypes} />

## Data Granularity

{entities.scheduleSummary.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleSummary.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleSummary.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleSummary.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleSummary.relationship}
  simple={true}
/>

{entities.scheduleSummary.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleSummary.additionalInfo) ? (
      entities.scheduleSummary.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleSummary.additionalInfo}</p>
    )}

  </>
)}
