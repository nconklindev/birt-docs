---
title: Self Schedule Request
description: The Data Dictionary entry for the Self Schedule Request entity
slug: /data-dictionary/self-schedule-request
---

import entities from "@site/src/data/entities.json";

# Self Schedule Request

{/* prettier-ignore */}
{Array.isArray(entities.selfScheduleRequest.description) ? (
  entities.selfScheduleRequest.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.selfScheduleRequest.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.selfScheduleRequest.supportedTypes} />

## Data Granularity

{entities.selfScheduleRequest.dataGranularity}

## Columns

<ColumnsTable columns={entities.selfScheduleRequest.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.selfScheduleRequest.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.selfScheduleRequest.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.selfScheduleRequest.relationship}
/>

{entities.selfScheduleRequest.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.selfScheduleRequest.additionalInfo) ? (
      entities.selfScheduleRequest.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.selfScheduleRequest.additionalInfo}</p>
    )}

  </>
)}
