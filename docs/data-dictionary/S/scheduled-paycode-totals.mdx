---
title: Scheduled Paycode Totals
description: The Data Dictionary entry for the Scheduled Paycode Totals entity
slug: /data-dictionary/scheduled-paycode-totals
---

import entities from "@site/src/data/entities.json";

# Scheduled Paycode Totals

{/* prettier-ignore */}
{Array.isArray(entities.scheduledPaycodeTotals.description) ? (
  entities.scheduledPaycodeTotals.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduledPaycodeTotals.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduledPaycodeTotals.supportedTypes} />

## Data Granularity

{entities.scheduledPaycodeTotals.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduledPaycodeTotals.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduledPaycodeTotals.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduledPaycodeTotals.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduledPaycodeTotals.relationship}
  simple={true}
/>

{entities.scheduledPaycodeTotals.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduledPaycodeTotals.additionalInfo) ? (
      entities.scheduledPaycodeTotals.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduledPaycodeTotals.additionalInfo}</p>
    )}

  </>
)}
