---
title: Schedule Tag Segment
description: The Data Dictionary entry for the Schedule Tag Segment entity
slug: /data-dictionary/schedule-tag-segment
---

import entities from "@site/src/data/entities.json";

# Schedule Tag Segment

{/* prettier-ignore */}
{Array.isArray(entities.scheduleTagSegment.description) ? (
  entities.scheduleTagSegment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.scheduleTagSegment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.scheduleTagSegment.supportedTypes} />

## Data Granularity

{entities.scheduleTagSegment.dataGranularity}

## Columns

<ColumnsTable columns={entities.scheduleTagSegment.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.scheduleTagSegment.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.scheduleTagSegment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.scheduleTagSegment.relationship}
  simple={true}
/>

{entities.scheduleTagSegment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.scheduleTagSegment.additionalInfo) ? (
      entities.scheduleTagSegment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.scheduleTagSegment.additionalInfo}</p>
    )}

  </>
)}
