---
title: Shift Segment Tag
description: The Data Dictionary entry for the Shift Segment Tag entity
slug: /data-dictionary/shift-segment-tag
---

import entities from "@site/src/data/entities.json";

# Shift Segment Tag

{/* prettier-ignore */}
{Array.isArray(entities.shiftSegmentTag.description) ? (
  entities.shiftSegmentTag.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.shiftSegmentTag.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.shiftSegmentTag.supportedTypes} />

## Data Granularity

{entities.shiftSegmentTag.dataGranularity}

## Columns

<ColumnsTable columns={entities.shiftSegmentTag.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.shiftSegmentTag.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.shiftSegmentTag.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.shiftSegmentTag.relationship}
  simple={true}
/>

{entities.shiftSegmentTag.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.shiftSegmentTag.additionalInfo) ? (
      entities.shiftSegmentTag.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.shiftSegmentTag.additionalInfo}</p>
    )}

  </>
)}
