---
title: Mobile App
description: The Data Dictionary entry for the Mobile App entity
slug: /data-dictionary/mobile-app
---

import entities from "@site/src/data/entities.json";

# Mobile App

{/* prettier-ignore */}
{Array.isArray(entities.mobileApp.description) ? (
  entities.mobileApp.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.mobileApp.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.mobileApp.supportedTypes} />

## Data Granularity

{entities.mobileApp.dataGranularity}

## Columns

<ColumnsTable columns={entities.mobileApp.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.mobileApp.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.mobileApp.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.mobileApp.relationship}
  simple={true}
/>

{entities.mobileApp.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.mobileApp.additionalInfo) ? (
      entities.mobileApp.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.mobileApp.additionalInfo}</p>
    )}

  </>
)}
