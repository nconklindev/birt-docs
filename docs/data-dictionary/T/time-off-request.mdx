---
title: Time Off Request
description: The Data Dictionary entry for the Time Off Request entity
slug: /data-dictionary/time-off-request
---

import entities from "@site/src/data/entities.json";

# Time Off Request

{/* prettier-ignore */}
{Array.isArray(entities.timeOffRequest.description) ? (
  entities.timeOffRequest.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.timeOffRequest.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.timeOffRequest.supportedTypes} />

## Data Granularity

{entities.timeOffRequest.dataGranularity}

## Columns

<ColumnsTable columns={entities.timeOffRequest.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.timeOffRequest.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.timeOffRequest.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.timeOffRequest.relationship}
  simple={true}
/>

{entities.timeOffRequest.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.timeOffRequest.additionalInfo) ? (
      entities.timeOffRequest.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.timeOffRequest.additionalInfo}</p>
    )}

  </>
)}
