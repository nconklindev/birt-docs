---
title: Traffic Pattern
description: The Data Dictionary entry for the Traffic Pattern entity
slug: /data-dictionary/traffic-pattern
---

import entities from "@site/src/data/entities.json";

# Traffic Pattern

{/* prettier-ignore */}
{Array.isArray(entities.trafficPattern.description) ? (
  entities.trafficPattern.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.trafficPattern.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.trafficPattern.supportedTypes} />

## Data Granularity

{entities.trafficPattern.dataGranularity}

## Columns

<ColumnsTable columns={entities.trafficPattern.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.trafficPattern.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.trafficPattern.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.trafficPattern.relationship}
  simple={true}
/>

{entities.trafficPattern.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.trafficPattern.additionalInfo) ? (
      entities.trafficPattern.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.trafficPattern.additionalInfo}</p>
    )}

  </>
)}
