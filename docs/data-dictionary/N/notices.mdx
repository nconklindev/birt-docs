---
title: Notices
description: The Data Dictionary entry for the Notices entity
slug: /data-dictionary/notices
---

import entities from "@site/src/data/entities.json";

# Notices

{/* prettier-ignore */}
{Array.isArray(entities.notices.description) ? (
  entities.notices.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.notices.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.notices.supportedTypes} />

## Data Granularity

{entities.notices.dataGranularity}

## Columns

<ColumnsTable columns={entities.notices.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.notices.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.notices.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.notices.relationship}
  simple={true}
/>

{entities.notices.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.notices.additionalInfo) ? (
      entities.notices.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.notices.additionalInfo}</p>
    )}

  </>
)}
