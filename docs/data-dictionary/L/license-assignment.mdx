---
title: License Assignment
description: The Data Dictionary entry for the License Assignment entity
slug: /data-dictionary/license-assignment
---

import entities from "@site/src/data/entities.json";

# License Assignment

{/* prettier-ignore */}
{Array.isArray(entities.licenseAssignment.description) ? (
  entities.licenseAssignment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.licenseAssignment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.licenseAssignment.supportedTypes} />

## Data Granularity

{entities.licenseAssignment.dataGranularity}

## Columns

<ColumnsTable columns={entities.licenseAssignment.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.licenseAssignment.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.licenseAssignment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.licenseAssignment.relationship}
  simple={true}
/>

{entities.licenseAssignment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.licenseAssignment.additionalInfo) ? (
      entities.licenseAssignment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.licenseAssignment.additionalInfo}</p>
    )}

  </>
)}
