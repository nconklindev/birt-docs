---
title: Leave Case Notification
description: The Data Dictionary entry for the Leave Case Notification entity
slug: /data-dictionary/leave-case-notification
---

import entities from "@site/src/data/entities.json";

# Leave Case Notification

{/* prettier-ignore */}
{Array.isArray(entities.leaveCaseNotification.description) ? (
  entities.leaveCaseNotification.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.leaveCaseNotification.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.leaveCaseNotification.supportedTypes} />

## Data Granularity

{entities.leaveCaseNotification.dataGranularity}

## Columns

<ColumnsTable columns={entities.leaveCaseNotification.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.leaveCaseNotification.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.leaveCaseNotification.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.leaveCaseNotification.relationship}
  simple={true}
/>

{entities.leaveCaseNotification.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.leaveCaseNotification.additionalInfo) ? (
      entities.leaveCaseNotification.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.leaveCaseNotification.additionalInfo}</p>
    )}

  </>
)}
