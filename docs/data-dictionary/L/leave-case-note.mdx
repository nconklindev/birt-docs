---
title: Leave Case Note
description: The Data Dictionary entry for the Leave Case Notes entity
slug: /data-dictionary/leave-case-note
---

import entities from "@site/src/data/entities.json";

# Leave Case Notes

{/* prettier-ignore */}
{Array.isArray(entities.leaveCaseNote.description) ? (
  entities.leaveCaseNote.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.leaveCaseNote.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.leaveCaseNote.supportedTypes} />

## Data Granularity

{entities.leaveCaseNote.dataGranularity}

## Columns

<ColumnsTable columns={entities.leaveCaseNote.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.leaveCaseNote.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.leaveCaseNote.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.leaveCaseNote.relationship}
  simple={true}
/>

{entities.leaveCaseNote.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.leaveCaseNote.additionalInfo) ? (
      entities.leaveCaseNote.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.leaveCaseNote.additionalInfo}</p>
    )}

  </>
)}
