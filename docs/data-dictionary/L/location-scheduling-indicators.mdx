---
title: Location Scheduling Indicators
description: The Data Dictionary entry for the Location Scheduling Indicators entity
slug: /data-dictionary/location-scheduling-indicators
---

import entities from "@site/src/data/entities.json";

# Location Scheduling Indicators

{/* prettier-ignore */}
{Array.isArray(entities.locationSchedulingIndicators.description) ? (
  entities.locationSchedulingIndicators.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.locationSchedulingIndicators.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.locationSchedulingIndicators.supportedTypes} />

## Data Granularity

{entities.locationSchedulingIndicators.dataGranularity}

## Columns

<ColumnsTable columns={entities.locationSchedulingIndicators.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.locationSchedulingIndicators.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.locationSchedulingIndicators.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.locationSchedulingIndicators.relationship}
  simple={true}
/>

{entities.locationSchedulingIndicators.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.locationSchedulingIndicators.additionalInfo) ? (
      entities.locationSchedulingIndicators.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.locationSchedulingIndicators.additionalInfo}</p>
    )}

  </>
)}
