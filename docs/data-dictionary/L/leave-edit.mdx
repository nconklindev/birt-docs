---
title: Leave Edit
description: The Data Dictionary entry for the Leave Edit entity
slug: /data-dictionary/leave-edit
---

import entities from "@site/src/data/entities.json";

# Leave Edit

{/* prettier-ignore */}
{Array.isArray(entities.leaveEdit.description) ? (
  entities.leaveEdit.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.leaveEdit.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.leaveEdit.supportedTypes} />

## Data Granularity

{entities.leaveEdit.dataGranularity}

## Columns

<ColumnsTable columns={entities.leaveEdit.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.leaveEdit.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.leaveEdit.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.leaveEdit.relationship}
  simple={true}
/>

{entities.leaveEdit.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.leaveEdit.additionalInfo) ? (
      entities.leaveEdit.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.leaveEdit.additionalInfo}</p>
    )}

  </>
)}
