---
title: Location
description: The Data Dictionary entry for the Location entity
slug: /data-dictionary/location
---

import entities from "@site/src/data/entities.json";

# Location

{/* prettier-ignore */}
{Array.isArray(entities.location.description) ? (
  entities.location.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.location.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.location.supportedTypes} />

## Data Granularity

{entities.location.dataGranularity}

## Columns

<ColumnsTable columns={entities.location.columns} />

## Related Entities (Employee)

<RelatedEntitiesList relatedEntities={entities.location.relatedEntities.employee} />

## Related Entities (Business Structure)

<RelatedEntitiesList relatedEntities={entities.location.relatedEntities.businessStructure} />

## Core Entity

<CoreEntityTable coreEntities={entities.location.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.location.relationship}
  simple={true}
/>

{entities.location.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.location.additionalInfo) ? (
      entities.location.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.location.additionalInfo}</p>
    )}

  </>
)}
