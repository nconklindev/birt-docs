---
title: Group Edit Results
description: The Data Dictionary entry for the Group Edit Results entity
slug: /data-dictionary/group-edit-results
---

import entities from "@site/src/data/entities.json";

# Group Edit Results

{/* prettier-ignore */}
{Array.isArray(entities.groupEditResults.description) ? (
  entities.groupEditResults.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.groupEditResults.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.groupEditResults.supportedTypes}
/>

## Data Granularity

{entities.groupEditResults.dataGranularity}

## Columns

<ColumnsTable columns={entities.groupEditResults.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.groupEditResults.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.groupEditResults.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.groupEditResults.relationship}
  simple={true}
/>

{entities.groupEditResults.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.groupEditResults.additionalInfo) ? (
      entities.groupEditResults.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.groupEditResults.additionalInfo}</p>
    )}

  </>
)}
