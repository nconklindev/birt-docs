---
title: Gate Access
description: The Data Dictionary entry for the Gate Access entity
slug: /data-dictionary/gate-access
---

import entities from "@site/src/data/entities.json";

# Gate Access

{/* prettier-ignore */}
{Array.isArray(entities.gateAccess.description) ? (
  entities.gateAccess.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.gateAccess.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.gateAccess.supportedTypes} />

## Data Granularity

{entities.gateAccess.dataGranularity}

## Columns

<ColumnsTable columns={entities.gateAccess.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.gateAccess.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.gateAccess.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.gateAccess.relationship}
  simple={true}
/>

{entities.gateAccess.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.gateAccess.additionalInfo) ? (
      entities.gateAccess.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.gateAccess.additionalInfo}</p>
    )}

  </>
)}
