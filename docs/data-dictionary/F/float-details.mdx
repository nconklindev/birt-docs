---
title: Float Details
description: The Data Dictionary entry for the Float Details entity
slug: /data-dictionary/float-details
---

import entities from "@site/src/data/entities.json";

# Float Details

{/* prettier-ignore */}
{Array.isArray(entities.floatDetails.description) ? (
  entities.floatDetails.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.floatDetails.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.floatDetails.supportedTypes} />

## Data Granularity

{entities.floatDetails.dataGranularity}

## Columns

<ColumnsTable columns={entities.floatDetails.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.floatDetails.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.floatDetails.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.floatDetails.relationship}
  simple={true}
/>

{entities.floatDetails.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.floatDetails.additionalInfo) ? (
      entities.floatDetails.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.floatDetails.additionalInfo}</p>
    )}

  </>
)}
