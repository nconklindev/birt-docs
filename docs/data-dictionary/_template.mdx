---
title: Entity Name
description: The Data Dictionary entry for the Entity Name entity
slug: /data-dictionary/entityName
---

import entities from "@site/src/data/entities.json";

# Entity Name

{/* prettier-ignore */}
{Array.isArray(entities.entityName.description) ? (
  entities.entityName.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.entityName.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.entityName.supportedTypes} />

## Data Granularity

{entities.entityName.dataGranularity}

## Columns

<ColumnsTable columns={entities.entityName.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.entityName.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.entityName.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.entityName.relationship}
  simple={true}
/>

{entities.entityName.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.entityName.additionalInfo) ? (
      entities.entityName.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.entityName.additionalInfo}</p>
    )}

  </>
)}
