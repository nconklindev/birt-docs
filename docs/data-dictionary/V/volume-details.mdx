---
title: Volume Details
description: The Data Dictionary entry for the Volume Details entity
slug: /data-dictionary/volume-details
---

import entities from "@site/src/data/entities.json";

# Volume Details

{/* prettier-ignore */}
{Array.isArray(entities.volumeDetails.description) ? (
  entities.volumeDetails.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.volumeDetails.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.volumeDetails.supportedTypes} />

## Data Granularity

{entities.volumeDetails.dataGranularity}

## Columns

<ColumnsTable columns={entities.volumeDetails.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.volumeDetails.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.volumeDetails.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.volumeDetails.relationship}
  simple={true}
/>

{entities.volumeDetails.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.volumeDetails.additionalInfo) ? (
      entities.volumeDetails.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.volumeDetails.additionalInfo}</p>
    )}

  </>
)}
