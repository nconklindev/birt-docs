---
title: Daily Productivity
description: The Data Dictionary entry for the Daily Productivity entity
---

import entities from "@site/src/data/entities.json";

# Daily Productivity

:::warning

The Healthcare Analytics entities and columns are available only from the API and cannot be used in Dataviews or to create reports at this time. To get the Healthcare Analytics data, use the Retrieve Data (POST /v1/commons/data/multi_read) API operation. Retrieve only the WORKUNIT and WORKUNIT_TIMESERIES data and ignore the Employee and Location data.

:::

{/* prettier-ignore */}
{Array.isArray(entities.dailyProductivity.description) ? (
  entities.dailyProductivity.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.dailyProductivity.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.dailyProductivity.supportedTypes} />

## Data Granularity (Work Unit)

{entities.dailyProductivity.dataGranularity.workUnit}

## Data Granularity (Work Unit Time Series)

{entities.dailyProductivity.dataGranularity.workUnitTimeSeries}

## Columns

<ColumnsTable columns={entities.dailyProductivity.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.dailyProductivity.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.dailyProductivity.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.dailyProductivity.relationship}
/>

{entities.dailyProductivity.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.dailyProductivity.additionalInfo) ? (
      entities.dailyProductivity.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.dailyProductivity.additionalInfo}</p>
    )}

  </>
)}
