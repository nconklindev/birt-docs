---
title: Payroll
description: The Data Dictionary entry for the Payroll entity
---

import entities from "@site/src/data/entities.json";

# Payroll

:::warning

The Healthcare Analytics entities and columns are available only from the API and cannot be used in Dataviews or to create reports at this time. To get the Healthcare Analytics data, use the Retrieve Data (POST /v1/commons/data/multi_read) API operation. Retrieve only the WORKUNIT and WORKUNIT_TIMESERIES data and ignore the Employee and Location data.

:::

{/* prettier-ignore */}
{Array.isArray(entities.payroll.description) ? (
  entities.payroll.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.payroll.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.payroll.supportedTypes} />

## Data Granularity

{entities.payroll.dataGranularity}

## Columns

<ColumnsTable columns={entities.payroll.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.payroll.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.payroll.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.payroll.relationship}
/>

{entities.payroll.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.payroll.additionalInfo) ? (
      entities.payroll.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.payroll.additionalInfo}</p>
    )}

  </>
)}
