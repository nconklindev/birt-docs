---
title: Work Group
description: The Data Dictionary entry for the Work Group entity
---

import entities from "@site/src/data/entities.json";

# Work Group

{/* prettier-ignore */}
{Array.isArray(entities.workGroup.description) ? (
  entities.workGroup.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.workGroup.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.workGroup.supportedTypes} />

## Data Granularity

{entities.workGroup.dataGranularity}

## Columns

<ColumnsTable columns={entities.workGroup.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.workGroup.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.workGroup.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.workGroup.relationship}
  simple={true}
/>

{entities.workGroup.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.workGroup.additionalInfo) ? (
      entities.workGroup.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.workGroup.additionalInfo}</p>
    )}

  </>
)}
