---
title: Pay Period Productivity
description: The Data Dictionary entry for the Pay Period Productivity entity
---

import entities from "@site/src/data/entities.json";

# Pay Period Productivity

:::warning

The Healthcare Analytics entities and columns are available only from the API and cannot be used in Dataviews or to create reports at this time. To get the Healthcare Analytics data, use the Retrieve Data (POST /v1/commons/data/multi_read) API operation. Retrieve only the WORKUNIT and WORKUNIT_TIMESERIES data and ignore the Employee and Location data.

:::

{/* prettier-ignore */}
{Array.isArray(entities.payPeriodProductivity.description) ? (
  entities.payPeriodProductivity.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.payPeriodProductivity.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.payPeriodProductivity.supportedTypes} />

## Data Granularity

{entities.payPeriodProductivity.dataGranularity}

## Columns

<ColumnsTable columns={entities.payPeriodProductivity.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.payPeriodProductivity.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.payPeriodProductivity.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.payPeriodProductivity.relationship}
/>

{entities.payPeriodProductivity.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.payPeriodProductivity.additionalInfo) ? (
      entities.payPeriodProductivity.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.payPeriodProductivity.additionalInfo}</p>
    )}

  </>
)}
