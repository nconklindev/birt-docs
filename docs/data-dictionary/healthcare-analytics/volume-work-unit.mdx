---
title: Volume Work Unit
description: The Data Dictionary entry for the Volume Work Unit entity
---

import entities from "@site/src/data/entities.json";

# Volume Work Unit

{/* prettier-ignore */}
{Array.isArray(entities.volumeWorkUnit.description) ? (
  entities.volumeWorkUnit.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.volumeWorkUnit.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.volumeWorkUnit.supportedTypes} />

## Data Granularity

{entities.volumeWorkUnit.dataGranularity}

## Columns

<ColumnsTable columns={entities.volumeWorkUnit.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.volumeWorkUnit.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.volumeWorkUnit.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.volumeWorkUnit.relationship}
/>

{entities.volumeWorkUnit.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.volumeWorkUnit.additionalInfo) ? (
      entities.volumeWorkUnit.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.volumeWorkUnit.additionalInfo}</p>
    )}

  </>
)}
