---
title: Volume
description: The Data Dictionary entry for the Volume entity
---

import entities from "@site/src/data/entities.json";

# Volume

{/* prettier-ignore */}
{Array.isArray(entities.volume.description) ? (
  entities.volume.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.volume.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.volume.supportedTypes} />

## Data Granularity

{entities.volume.dataGranularity}

## Columns

<ColumnsTable columns={entities.volume.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.volume.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.volume.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.volume.relationship}
/>

{entities.volume.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.volume.additionalInfo) ? (
      entities.volume.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.volume.additionalInfo}</p>
    )}

  </>
)}
