---
title: Worked Job
description: The Data Dictionary entry for the Worked Job entity
slug: /data-dictionary/worked-job
---

import entities from "@site/src/data/entities.json";

# Worked Job

{/* prettier-ignore */}
{Array.isArray(entities.workedJob.description) ? (
  entities.workedJob.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.workedJob.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.workedJob.supportedTypes} />

## Data Granularity

{entities.workedJob.dataGranularity}

## Columns

<ColumnsTable columns={entities.workedJob.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.workedJob.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.workedJob.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.workedJob.relationship}
  simple={true}
/>

{entities.workedJob.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.workedJob.additionalInfo) ? (
      entities.workedJob.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.workedJob.additionalInfo}</p>
    )}

  </>
)}
