---
title: Primary Job Assignment History
description: The Data Dictionary entry for the Primary Job Assignment History entity
slug: /data-dictionary/primary-job-assignment-history
---

import entities from "@site/src/data/entities.json";

# Primary Job Assignment History

{/* prettier-ignore */}
{Array.isArray(entities.primaryJobAssignmentHistory.description) ? (
  entities.primaryJobAssignmentHistory.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.primaryJobAssignmentHistory.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.primaryJobAssignmentHistory.supportedTypes} />

## Data Granularity

{entities.primaryJobAssignmentHistory.dataGranularity}

## Columns

<ColumnsTable columns={entities.primaryJobAssignmentHistory.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.primaryJobAssignmentHistory.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.primaryJobAssignmentHistory.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.primaryJobAssignmentHistory.relationship}
  simple={true}
/>

{entities.primaryJobAssignmentHistory.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.primaryJobAssignmentHistory.additionalInfo) ? (
      entities.primaryJobAssignmentHistory.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.primaryJobAssignmentHistory.additionalInfo}</p>
    )}

  </>
)}
