---
title: Paycode
description: The Data Dictionary entry for the Paycode entity
slug: /data-dictionary/paycode
---

import entities from "@site/src/data/entities.json";

# Paycode

{/* prettier-ignore */}
{Array.isArray(entities.paycode.description) ? (
  entities.paycode.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.paycode.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.paycode.supportedTypes} />

## Data Granularity

{entities.paycode.dataGranularity}

## Columns

<ColumnsTable columns={entities.paycode.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.paycode.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.paycode.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.paycode.relationship}
  simple={true}
/>

{entities.paycode.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.paycode.additionalInfo) ? (
      entities.paycode.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.paycode.additionalInfo}</p>
    )}

  </>
)}
