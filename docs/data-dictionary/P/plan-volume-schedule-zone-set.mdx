---
title: Plan Volume - Schedule Zone Set
description: The Data Dictionary entry for the Plan Volume - Schedule Zone Set entity
slug: /data-dictionary/plan-volume-schedule-zone-set
---

import entities from "@site/src/data/entities.json";

# Plan Volume - Schedule Zone Set

{/* prettier-ignore */}
{Array.isArray(entities.planVolumeScheduleZoneSet.description) ? (
  entities.planVolumeScheduleZoneSet.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.planVolumeScheduleZoneSet.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.planVolumeScheduleZoneSet.supportedTypes} />

## Data Granularity

{entities.planVolumeScheduleZoneSet.dataGranularity}

## Columns

<ColumnsTable columns={entities.planVolumeScheduleZoneSet.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.planVolumeScheduleZoneSet.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.planVolumeScheduleZoneSet.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.planVolumeScheduleZoneSet.relationship}
  simple={true}
/>

{entities.planVolumeScheduleZoneSet.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.planVolumeScheduleZoneSet.additionalInfo) ? (
      entities.planVolumeScheduleZoneSet.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.planVolumeScheduleZoneSet.additionalInfo}</p>
    )}

  </>
)}
