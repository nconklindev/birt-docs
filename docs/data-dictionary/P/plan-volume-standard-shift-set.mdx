---
title: Plan Volume - Standard Shift Set
description: The Data Dictionary entry for the Plan Volume - Standard Shift Set entity
slug: /data-dictionary/plan-volume-standard-shift-set
---

import entities from "@site/src/data/entities.json";

# Plan Volume - Standard Shift Set

{/* prettier-ignore */}
{Array.isArray(entities.planVolumeStandardShiftSet.description) ? (
  entities.planVolumeStandardShiftSet.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.planVolumeStandardShiftSet.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.planVolumeStandardShiftSet.supportedTypes} />

## Data Granularity

{entities.planVolumeStandardShiftSet.dataGranularity}

## Columns

<ColumnsTable columns={entities.planVolumeStandardShiftSet.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.planVolumeStandardShiftSet.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.planVolumeStandardShiftSet.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.planVolumeStandardShiftSet.relationship}
  simple={true}
/>

{entities.planVolumeStandardShiftSet.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.planVolumeStandardShiftSet.additionalInfo) ? (
      entities.planVolumeStandardShiftSet.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.planVolumeStandardShiftSet.additionalInfo}</p>
    )}

  </>
)}
