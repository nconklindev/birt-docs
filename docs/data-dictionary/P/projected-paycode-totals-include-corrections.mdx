---
title: Projected Paycode Totals (Include Corrections)
description: The Data Dictionary entry for the Projected Paycode Totals (Include Corrections) entity
slug: /data-dictionary/projected-paycode-totals-include-corrections
---

import entities from "@site/src/data/entities.json";

# Projected Paycode Totals (Include Corrections)

{/* prettier-ignore */}
{Array.isArray(entities.projectedPaycodeTotalsIncludeCorrections.description) ? (
  entities.projectedPaycodeTotalsIncludeCorrections.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.projectedPaycodeTotalsIncludeCorrections.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.projectedPaycodeTotalsIncludeCorrections.supportedTypes} />

## Data Granularity

{entities.projectedPaycodeTotalsIncludeCorrections.dataGranularity}

## Columns

<ColumnsTable columns={entities.projectedPaycodeTotalsIncludeCorrections.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.projectedPaycodeTotalsIncludeCorrections.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.projectedPaycodeTotalsIncludeCorrections.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.projectedPaycodeTotalsIncludeCorrections.relationship}
  simple={true}
/>

{entities.projectedPaycodeTotalsIncludeCorrections.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.projectedPaycodeTotalsIncludeCorrections.additionalInfo) ? (
      entities.projectedPaycodeTotalsIncludeCorrections.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.projectedPaycodeTotalsIncludeCorrections.additionalInfo}</p>
    )}

  </>
)}
