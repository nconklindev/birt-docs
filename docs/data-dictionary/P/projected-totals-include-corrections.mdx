---
title: Projected Totals (Include Corrections)
description: The Data Dictionary entry for the Projected Totals (Include Corrections) entity
slug: /data-dictionary/projected-totals-include-corrections
---

import entities from "@site/src/data/entities.json";

# Projected Totals (Include Corrections)

{/* prettier-ignore */}
{Array.isArray(entities.projectedTotalsIncludeCorrections.description) ? (
  entities.projectedTotalsIncludeCorrections.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.projectedTotalsIncludeCorrections.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.projectedTotalsIncludeCorrections.supportedTypes} />

## Data Granularity

{entities.projectedTotalsIncludeCorrections.dataGranularity}

## Columns

<ColumnsTable columns={entities.projectedTotalsIncludeCorrections.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.projectedTotalsIncludeCorrections.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.projectedTotalsIncludeCorrections.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.projectedTotalsIncludeCorrections.relationship}
  simple={true}
/>

{entities.projectedTotalsIncludeCorrections.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.projectedTotalsIncludeCorrections.additionalInfo) ? (
      entities.projectedTotalsIncludeCorrections.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.projectedTotalsIncludeCorrections.additionalInfo}</p>
    )}

  </>
)}
