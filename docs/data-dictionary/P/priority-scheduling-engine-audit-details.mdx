---
title: Priority Scheduling Engine Audit Details
description: The Data Dictionary entry for the Priority Scheduling Engine Audit Details entity
slug: /data-dictionary/priority-scheduling-engine-audit-details
---

import entities from "@site/src/data/entities.json";

# Priority Scheduling Engine Audit Details

{/* prettier-ignore */}
{Array.isArray(entities.prioritySchedulingEngineAuditDetails.description) ? (
  entities.prioritySchedulingEngineAuditDetails.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.prioritySchedulingEngineAuditDetails.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.prioritySchedulingEngineAuditDetails.supportedTypes} />

## Data Granularity

{entities.prioritySchedulingEngineAuditDetails.dataGranularity}

## Columns

<ColumnsTable columns={entities.prioritySchedulingEngineAuditDetails.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.prioritySchedulingEngineAuditDetails.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.prioritySchedulingEngineAuditDetails.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.prioritySchedulingEngineAuditDetails.relationship}
  simple={true}
/>

{entities.prioritySchedulingEngineAuditDetails.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.prioritySchedulingEngineAuditDetails.additionalInfo) ? (
      entities.prioritySchedulingEngineAuditDetails.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.prioritySchedulingEngineAuditDetails.additionalInfo}</p>
    )}

  </>
)}
