---
title: Posted Schedule
description: The Data Dictionary entry for the Posted Schedule entity
slug: /data-dictionary/posted-schedule
---

import entities from "@site/src/data/entities.json";

# Posted Schedule

{/* prettier-ignore */}
{Array.isArray(entities.postedSchedule.description) ? (
  entities.postedSchedule.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.postedSchedule.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.postedSchedule.supportedTypes} />

## Data Granularity

{entities.postedSchedule.dataGranularity}

## Columns

<ColumnsTable columns={entities.postedSchedule.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.postedSchedule.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.postedSchedule.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.postedSchedule.relationship}
  simple={true}
/>

{entities.postedSchedule.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.postedSchedule.additionalInfo) ? (
      entities.postedSchedule.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.postedSchedule.additionalInfo}</p>
    )}

  </>
)}
