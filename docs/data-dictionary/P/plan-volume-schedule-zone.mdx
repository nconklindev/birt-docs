---
title: Plan Volume - Schedule Zone
description: The Data Dictionary entry for the Plan Volume - Schedule Zone entity
slug: /data-dictionary/plan-volume-schedule-zone
---

import entities from "@site/src/data/entities.json";

# Plan Volume - Schedule Zone

{/* prettier-ignore */}
{Array.isArray(entities.planVolumeScheduleZone.description) ? (
  entities.planVolumeScheduleZone.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.planVolumeScheduleZone.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.planVolumeScheduleZone.supportedTypes} />

## Data Granularity

{entities.planVolumeScheduleZone.dataGranularity}

## Columns

<ColumnsTable columns={entities.planVolumeScheduleZone.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.planVolumeScheduleZone.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.planVolumeScheduleZone.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.planVolumeScheduleZone.relationship}
  simple={true}
/>

{entities.planVolumeScheduleZone.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.planVolumeScheduleZone.additionalInfo) ? (
      entities.planVolumeScheduleZone.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.planVolumeScheduleZone.additionalInfo}</p>
    )}

  </>
)}
