---
title: Coverage Detail - Scheduled Count
description: The Data Dictionary entry for the Coverage Detail - Scheduled Count entity
slug: /data-dictionary/coverage-detail-scheduled-count
---

import entities from "@site/src/data/entities.json";

# Coverage Detail - Scheduled Count

{/* prettier-ignore */}
{Array.isArray(entities.coverageDetailScheduledCount.description) ? (
  entities.coverageDetailScheduledCount.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.coverageDetailScheduledCount.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.coverageDetailScheduledCount.supportedTypes}
/>

## Data Granularity

{entities.coverageDetailScheduledCount.dataGranularity}

## Columns

<ColumnsTable columns={entities.coverageDetailScheduledCount.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.coverageDetailScheduledCount.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.coverageDetailScheduledCount.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.coverageDetailScheduledCount.relationship}
  simple={true}
/>

{entities.coverageDetailScheduledCount.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.coverageDetailScheduledCount.additionalInfo) ? (
      entities.coverageDetailScheduledCount.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.coverageDetailScheduledCount.additionalInfo}</p>
    )}

  </>
)}
