---
title: Cover Request
description: The Data Dictionary entry for the Cover Request entity
slug: /data-dictionary/cover-request
---

import entities from "@site/src/data/entities.json";

# Cover Request

{/* prettier-ignore */}
{Array.isArray(entities.coverRequest.description) ? (
  entities.coverRequest.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.coverRequest.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.coverRequest.supportedTypes} />

## Data Granularity

{entities.coverRequest.dataGranularity}

## Columns

<ColumnsTable columns={entities.coverRequest.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.coverRequest.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.coverRequest.coreEntity} />

## Relationship

<RelationshipTable relationship={entities.coverRequest.relationship} />

{entities.coverRequest.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.coverRequest.additionalInfo) ? (
      entities.coverRequest.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.coverRequest.additionalInfo}</p>
    )}

  </>
)}
