---
title: Employee Skill Assignment
description: The Data Dictionary entry for the Employee Skill Assignment entity
slug: /data-dictionary/employee-skill-assignment
---

import entities from "@site/src/data/entities.json";

# Employee Skill Assignment

{/* prettier-ignore */}
{Array.isArray(entities.employeeSkillAssignment.description) ? (
  entities.employeeSkillAssignment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeSkillAssignment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.employeeSkillAssignment.supportedTypes}
/>

## Data Granularity

{entities.employeeSkillAssignment.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeSkillAssignment.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.employeeSkillAssignment.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.employeeSkillAssignment.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeSkillAssignment.relationship}
  simple={true}
/>

{entities.employeeSkillAssignment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeSkillAssignment.additionalInfo) ? (
      entities.employeeSkillAssignment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeSkillAssignment.additionalInfo}</p>
    )}

  </>
)}
