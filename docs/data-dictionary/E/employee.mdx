---
title: Employee
description: The Data Dictionary entry for the Employee entity
slug: /data-dictionary/employee
---

import entities from "@site/src/data/entities.json";

# Employee

{/* prettier-ignore */}
{Array.isArray(entities.employee.description) ? (
  entities.employee.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employee.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employee.supportedTypes} />

## Data Granularity

{entities.employee.dataGranularity}

## Columns

<ColumnsTable columns={entities.employee.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employee.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employee.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employee.relationship}
  simple={true}
/>

{entities.employee.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employee.additionalInfo) ? (
      entities.employee.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employee.additionalInfo}</p>
    )}

  </>
)}
