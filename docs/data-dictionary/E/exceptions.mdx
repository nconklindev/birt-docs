---
title: Exceptions
description: The Data Dictionary entry for the Exceptions entity
slug: /data-dictionary/exceptions
---

import entities from "@site/src/data/entities.json";

# Exceptions

{/* prettier-ignore */}
{Array.isArray(entities.exceptions.description) ? (
  entities.exceptions.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.exceptions.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.exceptions.supportedTypes} />

## Data Granularity

{entities.exceptions.dataGranularity}

## Columns

<ColumnsTable columns={entities.exceptions.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.exceptions.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.exceptions.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.exceptions.relationship}
  simple={true}
/>

{entities.exceptions.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.exceptions.additionalInfo) ? (
      entities.exceptions.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.exceptions.additionalInfo}</p>
    )}

  </>
)}
