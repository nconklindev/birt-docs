---
title: Employee Certification Assignment
description: The Data Dictionary entry for the Employee Certification Assignment entity
slug: /data-dictionary/employee-certification-assignment
---

import entities from "@site/src/data/entities.json";

# Employee Certification Assignment

{/* prettier-ignore */}
{Array.isArray(entities.employeeCertificationAssignment.description) ? (
  entities.employeeCertificationAssignment.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeCertificationAssignment.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.employeeCertificationAssignment.supportedTypes}
/>

## Data Granularity

{entities.employeeCertificationAssignment.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeCertificationAssignment.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.employeeCertificationAssignment.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.employeeCertificationAssignment.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.employeeCertificationAssignment.relationship}
  simple={true}
/>

{entities.employeeCertificationAssignment.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeCertificationAssignment.additionalInfo) ? (
      entities.employeeCertificationAssignment.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeCertificationAssignment.additionalInfo}</p>
    )}

  </>
)}
