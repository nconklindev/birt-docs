---
title: Employee Details
description: The Data Dictionary entry for the Employee Details entity
slug: /data-dictionary/employee-details
---

import entities from "@site/src/data/entities.json";

# Employee Details

{/* prettier-ignore */}
{Array.isArray(entities.employeeDetails.description) ? (
  entities.employeeDetails.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeDetails.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeDetails.supportedTypes} />

## Data Granularity

{entities.employeeDetails.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeDetails.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.employeeDetails.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.employeeDetails.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeDetails.relationship}
  simple={true}
/>

{entities.employeeDetails.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeDetails.additionalInfo) ? (
      entities.employeeDetails.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeDetails.additionalInfo}</p>
    )}

  </>
)}
