---
title: Perspective Score
description: The Data Dictionary entry for the Perspective Score entity
---

import entities from "@site/src/data/entities.json";

# Perspective Score

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmPerspectiveScore.description) ? (
  entities.employeeHcmPerspectiveScore.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmPerspectiveScore.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmPerspectiveScore.supportedTypes} />

## Data Granularity

{entities.employeeHcmPerspectiveScore.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmPerspectiveScore.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmPerspectiveScore.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmPerspectiveScore.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmPerspectiveScore.relationship}
  simple={true}
/>

{entities.employeeHcmPerspectiveScore.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmPerspectiveScore.additionalInfo) ? (
      entities.employeeHcmPerspectiveScore.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmPerspectiveScore.additionalInfo}</p>
    )}

  </>
)}
