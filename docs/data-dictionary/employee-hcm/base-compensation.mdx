---
title: Base Compensation
description: The Data Dictionary entry for the Base Compensation entity

---

import entities from "@site/src/data/entities.json";

# Base Compensation

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmBaseCompensation.description) ? (
  entities.employeeHcmBaseCompensation.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmBaseCompensation.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmBaseCompensation.supportedTypes} />

## Data Granularity

{entities.employeeHcmBaseCompensation.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmBaseCompensation.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmBaseCompensation.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmBaseCompensation.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmBaseCompensation.relationship}
  simple={true}
/>

{entities.employeeHcmBaseCompensation.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmBaseCompensation.additionalInfo) ? (
      entities.employeeHcmBaseCompensation.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmBaseCompensation.additionalInfo}</p>
    )}

  </>
)}
