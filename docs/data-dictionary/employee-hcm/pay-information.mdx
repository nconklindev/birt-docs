---
title: Pay Information
description: The Data Dictionary entry for the Pay Information entity
---

import entities from "@site/src/data/entities.json";

# Pay Information

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmPayInformation.description) ? (
  entities.employeeHcmPayInformation.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmPayInformation.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmPayInformation.supportedTypes} />

## Data Granularity

{entities.employeeHcmPayInformation.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmPayInformation.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmPayInformation.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmPayInformation.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmPayInformation.relationship}
  simple={true}
/>

{entities.employeeHcmPayInformation.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmPayInformation.additionalInfo) ? (
      entities.employeeHcmPayInformation.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmPayInformation.additionalInfo}</p>
    )}

  </>
)}
