---
title: Performance Review
description: The Data Dictionary entry for the Performance Review entity
---

import entities from "@site/src/data/entities.json";

# Performance Review

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmPerformanceReview.description) ? (
  entities.employeeHcmPerformanceReview.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmPerformanceReview.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmPerformanceReview.supportedTypes} />

## Data Granularity

{entities.employeeHcmPerformanceReview.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmPerformanceReview.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmPerformanceReview.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmPerformanceReview.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmPerformanceReview.relationship}
  simple={true}
/>

{entities.employeeHcmPerformanceReview.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmPerformanceReview.additionalInfo) ? (
      entities.employeeHcmPerformanceReview.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmPerformanceReview.additionalInfo}</p>
    )}

  </>
)}
