---
title: Personal Information
description: The Data Dictionary entry for the Personal Information entity
---

import entities from "@site/src/data/entities.json";

# Personal Information

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmPersonalInformation.description) ? (
  entities.employeeHcmPersonalInformation.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmPersonalInformation.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmPersonalInformation.supportedTypes} />

## Data Granularity

{entities.employeeHcmPersonalInformation.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmPersonalInformation.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmPersonalInformation.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmPersonalInformation.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmPersonalInformation.relationship}
  simple={true}
/>

{entities.employeeHcmPersonalInformation.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmPersonalInformation.additionalInfo) ? (
      entities.employeeHcmPersonalInformation.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmPersonalInformation.additionalInfo}</p>
    )}

  </>
)}
