---
title: Termination Details
description: The Data Dictionary entry for the Termination Details entity
---

import entities from "@site/src/data/entities.json";

# Termination Details

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmTerminationDetails.description) ? (
  entities.employeeHcmTerminationDetails.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmTerminationDetails.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmTerminationDetails.supportedTypes} />

## Data Granularity

{entities.employeeHcmTerminationDetails.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmTerminationDetails.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmTerminationDetails.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmTerminationDetails.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmTerminationDetails.relationship}
  simple={true}
/>

{entities.employeeHcmTerminationDetails.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmTerminationDetails.additionalInfo) ? (
      entities.employeeHcmTerminationDetails.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmTerminationDetails.additionalInfo}</p>
    )}

  </>
)}
