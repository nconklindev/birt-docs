---
title: Job Changes History
description: The Data Dictionary entry for the Job Changes History entity
---

import entities from "@site/src/data/entities.json";

# Job Changes History

{/* prettier-ignore */}
{Array.isArray(entities.employeeHcmJobChangesHistory.description) ? (
  entities.employeeHcmJobChangesHistory.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.employeeHcmJobChangesHistory.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.employeeHcmJobChangesHistory.supportedTypes} />

## Data Granularity

{entities.employeeHcmJobChangesHistory.dataGranularity}

## Columns

<ColumnsTable columns={entities.employeeHcmJobChangesHistory.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.employeeHcmJobChangesHistory.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.employeeHcmJobChangesHistory.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.employeeHcmJobChangesHistory.relationship}
  simple={true}
/>

{entities.employeeHcmJobChangesHistory.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.employeeHcmJobChangesHistory.additionalInfo) ? (
      entities.employeeHcmJobChangesHistory.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.employeeHcmJobChangesHistory.additionalInfo}</p>
    )}

  </>
)}
