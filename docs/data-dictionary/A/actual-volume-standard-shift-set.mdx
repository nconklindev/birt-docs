---
title: Actual Volume Standard Shift Set
description: The Data Dictionary entry for the Actual Volume Standard Shift Set entity
slug: /data-dictionary/actual-volume-standard-shift-set
---

import entities from "@site/src/data/entities.json";

# Actual Volume Standard Shift Set

{Array.isArray(entities.actualVolumeStandardShiftSet.description) ? (
entities.actualVolumeStandardShiftSet.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.actualVolumeStandardShiftSet.description.split('\n').map((paragraph,
index) => (<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualVolumeStandardShiftSet.supportedTypes}
/>

## Data Granularity

{entities.actualVolumeStandardShiftSet.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualVolumeStandardShiftSet.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.actualVolumeStandardShiftSet.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.actualVolumeStandardShiftSet.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.actualVolumeStandardShiftSet.relationship}
  simple={true}
/>

{entities.actualVolumeStandardShiftSet.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualVolumeStandardShiftSet.additionalInfo) ? (
      entities.actualVolumeStandardShiftSet.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualVolumeStandardShiftSet.additionalInfo}</p>
    )}

  </>
)}
