---
title: Actual Totals (Exclude Corrections)
description: The Data Dictionary entry for the Actual Totals (Exclude Corrections) entity
slug: /data-dictionary/actual-totals-exclude-corrections
---

import entities from "@site/src/data/entities.json";

# Actual Totals (Exclude Corrections)

{Array.isArray(entities.actualTotalsExcludeCorrections.description) ? (
entities.actualTotalsExcludeCorrections.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.actualTotalsExcludeCorrections.description.split('\n').map((paragraph,
index) => (<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualTotalsExcludeCorrections.supportedTypes}
/>

## Data Granularity

{entities.actualTotalsExcludeCorrections.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualTotalsExcludeCorrections.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.actualTotalsExcludeCorrections.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.actualTotalsExcludeCorrections.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.actualTotalsExcludeCorrections.relationship}
  simple={true}
/>

{entities.actualTotalsExcludeCorrections.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualTotalsExcludeCorrections.additionalInfo) ? (
      entities.actualTotalsExcludeCorrections.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualTotalsExcludeCorrections.additionalInfo}</p>
    )}

  </>
)}
