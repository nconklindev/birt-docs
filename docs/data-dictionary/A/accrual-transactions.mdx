---
title: Accrual Transactions
description: The Data Dictionary entry for the Accrual Transactions entity
slug: /data-dictionary/accrual-transactions
---

import entities from "@site/src/data/entities.json";

# Accrual Transactions

{Array.isArray(entities.accrualTransactions.description) ? (
entities.accrualTransactions.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.accrualTransactions.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.accrualTransactions.supportedTypes}
/>

## Data Granularity

{entities.accrualTransactions.dataGranularity}

## Columns

<ColumnsTable columns={entities.accrualTransactions.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.accrualTransactions.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.accrualTransactions.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.accrualTransactions.relationship}
  simple={true}
/>

{entities.accrualTransactions.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.accrualTransactions.additionalInfo) ? (
      entities.accrualTransactions.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.accrualTransactions.additionalInfo}</p>
    )}

  </>
)}
