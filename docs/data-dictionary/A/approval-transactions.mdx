---
title: Approval Transactions
description: The Data Dictionary entry for the Approval Transactions entity
slug: /data-dictionary/approval-transactions
---

import entities from "@site/src/data/entities.json";

# Approval Transactions

{Array.isArray(entities.approvalsReviewers.description) ? (
entities.approvalsReviewers.description.map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
) : (
entities.approvalsReviewers.description.split('\n').map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.approvalTransactions.supportedTypes}
/>

## Data Granularity

{entities.approvalTransactions.dataGranularity}

## Columns

<ColumnsTable columns={entities.approvalTransactions.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.approvalTransactions.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.approvalTransactions.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.approvalTransactions.relationship}
  simple={true}
/>

{entities.approvalTransactions.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.approvalTransactions.additionalInfo) ? (
      entities.approvalTransactions.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.approvalTransactions.additionalInfo}</p>
    )}

  </>
)}
