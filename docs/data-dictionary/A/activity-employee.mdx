---
title: Activity Employee
description: The Data Dictionary entry for the Activity Employee entity
slug: /data-dictionary/activity-employee
---

import entities from "@site/src/data/entities.json";

# Activity Employee

{Array.isArray(entities.activityEmployee.description) ? (
entities.activityEmployee.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityEmployee.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.activityEmployee.supportedTypes}
/>

## Data Granularity

{entities.activityEmployee.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityEmployee.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.activityEmployee.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.activityEmployee.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.activityEmployee.relationship}
  simple={false}
/>

{entities.activityEmployee.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activityEmployee.additionalInfo) ? (
      entities.activityEmployee.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityEmployee.additionalInfo}</p>
    )}

  </>
)}
