---
title: Actual Paycode Totals (Exclude Corrections)
description: The Data Dictionary entry for the Actual Paycode Totals (Exclude Corrections) entity
slug: /data-dictionary/actual-paycode-totals-exclude-corrections
---

import entities from "@site/src/data/entities.json";

# Actual Paycode Totals (Exclude Corrections)

{Array.isArray(entities.actualPaycodeTotalsExcludeCorrections.description) ? (
entities.actualPaycodeTotalsExcludeCorrections.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.actualPaycodeTotalsExcludeCorrections.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualPaycodeTotalsExcludeCorrections.supportedTypes}
/>

## Data Granularity

{entities.actualPaycodeTotalsExcludeCorrections.dataGranularity}

## Columns

<ColumnsTable
  columns={entities.actualPaycodeTotalsExcludeCorrections.columns}
/>

## Related Entities

<RelatedEntitiesList
  relatedEntities={
    entities.actualPaycodeTotalsExcludeCorrections.relatedEntities
  }
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.actualPaycodeTotalsExcludeCorrections.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.actualPaycodeTotalsExcludeCorrections.relationship}
  simple={true}
/>

{entities.actualPaycodeTotalsExcludeCorrections.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualPaycodeTotalsExcludeCorrections.additionalInfo) ? (
      entities.actualPaycodeTotalsExcludeCorrections.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualPaycodeTotalsExcludeCorrections.additionalInfo}</p>
    )}

  </>
)}
