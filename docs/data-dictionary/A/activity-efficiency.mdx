---
title: Activity Efficiency
description: The Data Dictionary entry for the Activity Efficiency entity
slug: /data-dictionary/activity-efficiency
---

import entities from "@site/src/data/entities.json";

# Activity Efficiency

{Array.isArray(entities.activityEfficiency.description) ? (
entities.activityEfficiency.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityEfficiency.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.activityEfficiency.supportedTypes}
/>

## Data Granularity

{entities.activityEfficiency.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityEfficiency.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.activityEfficiency.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.activityEfficiency.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.activityEfficiency.relationship}
  simple={false}
/>

{entities.activityEfficiency.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activityEfficiency.additionalInfo) ? (
      entities.activityEfficiency.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityEfficiency.additionalInfo}</p>
    )}

  </>
)}
