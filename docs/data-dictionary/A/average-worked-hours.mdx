---
title: Average Worked Hours
description: The Data Dictionary entry for the Average Worked Hours entity
slug: /data-dictionary/average-worked-hours
---

import entities from "@site/src/data/entities.json";

# Average Worked Hours

{/* prettier-ignore */}
{Array.isArray(entities.averageWorkedHours.description) ? (
  entities.averageWorkedHours.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.averageWorkedHours.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.averageWorkedHours.supportedTypes}
/>

## Data Granularity

{entities.averageWorkedHours.dataGranularity}

## Columns

<ColumnsTable columns={entities.averageWorkedHours.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.averageWorkedHours.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.averageWorkedHours.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.averageWorkedHours.relationship}
  simple={true}
/>

{entities.averageWorkedHours.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.averageWorkedHours.additionalInfo) ? (
      entities.averageWorkedHours.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.averageWorkedHours.additionalInfo}</p>
    )}

  </>
)}
