---
title: Audit
description: The Data Dictionary entry for the Audit entity
slug: /data-dictionary/audit
---

import entities from "@site/src/data/entities.json";

# Audit

{/* prettier-ignore */}
{Array.isArray(entities.audit.description) ? (
  entities.audit.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.audit.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.audit.supportedTypes} />

## Data Granularity

{entities.audit.dataGranularity}

## Columns

<ColumnsTable columns={entities.audit.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.audit.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.audit.coreEntity} />

## Relationship

<RelationshipTable relationship={entities.audit.relationship} simple={true} />

{entities.audit.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.audit.additionalInfo) ? (
      entities.audit.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.audit.additionalInfo}</p>
    )}

  </>
)}
