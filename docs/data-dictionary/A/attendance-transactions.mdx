---
title: Attendance Transactions
description: The Data Dictionary entry for the Attendance Transactions entity
slug: /data-dictionary/attendance-transactions
---

import entities from "@site/src/data/entities.json";

# Attendance Transactions

{/* prettier-ignore */}
{Array.isArray(entities.attendanceTransactions.description) ? (
  entities.attendanceTransactions.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.attendanceTransactions.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.attendanceTransactions.supportedTypes}
/>

## Data Granularity

{entities.attendanceTransactions.dataGranularity}

## Columns

<ColumnsTable columns={entities.attendanceTransactions.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.attendanceTransactions.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.attendanceTransactions.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.attendanceTransactions.relationship}
  simple={true}
/>

{entities.attendanceTransactions.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.attendanceTransactions.additionalInfo) ? (
      entities.attendanceTransactions.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.attendanceTransactions.additionalInfo}</p>
    )}

  </>
)}
