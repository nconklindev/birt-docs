---
title: Activity Failed Forms
description: The Data Dictionary entry for the Activity Failed Forms entity
slug: /data-dictionary/activity-failed-forms
---

import entities from "@site/src/data/entities.json";

# Activity Failed Forms

{Array.isArray(entities.activityFailedForms.description) ? (
entities.activityFailedForms.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityFailedForms.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.activityFailedForms.supportedTypes}
/>

## Data Granularity

{entities.activityFailedForms.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityFailedForms.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.activityFailedForms.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.activityFailedForms.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.activityFailedForms.relationship}
  simple={true}
/>

{entities.activityFailedForms.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activityFailedForms.additionalInfo) ? (
      entities.activityFailedForms.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityFailedForms.additionalInfo}</p>
    )}

  </>
)}
