---
title: Auditor Count - Month
description: The Data Dictionary entry for the Auditor Count - Month entity
slug: /data-dictionary/auditor-count-month
---

import entities from "@site/src/data/entities.json";

# Auditor Count - Month

{/* prettier-ignore */}
{Array.isArray(entities.auditorCountMonth.description) ? (
  entities.auditorCountMonth.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.auditorCountMonth.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.auditorCountMonth.supportedTypes}
/>

## Data Granularity

{entities.auditorCountMonth.dataGranularity}

## Columns

<ColumnsTable columns={entities.auditorCountMonth.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.auditorCountMonth.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.auditorCountMonth.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.auditorCountMonth.relationship}
  simple={true}
/>

{entities.auditorCountMonth.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.auditorCountMonth.additionalInfo) ? (
      entities.auditorCountMonth.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.auditorCountMonth.additionalInfo}</p>
    )}

  </>
)}
