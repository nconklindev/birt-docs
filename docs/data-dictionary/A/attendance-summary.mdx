---
title: Attendance Summary
description: The Data Dictionary entry for the Attendance Summary entity
slug: /data-dictionary/attendance-summary
---

import entities from "@site/src/data/entities.json";

# Attendance Summary

{Array.isArray(entities.attendanceSummary.description) ? (
entities.attendanceSummary.description.map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
) : (
entities.attendanceSummary.description.split('\n').map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.attendanceSummary.supportedTypes}
/>

## Data Granularity

{entities.attendanceSummary.dataGranularity}

## Columns

<ColumnsTable columns={entities.attendanceSummary.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.attendanceSummary.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.attendanceSummary.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.attendanceSummary.relationship}
  simple={true}
/>

{entities.attendanceSummary.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.attendanceSummary.additionalInfo) ? (
      entities.attendanceSummary.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.attendanceSummary.additionalInfo}</p>
    )}

  </>
)}
