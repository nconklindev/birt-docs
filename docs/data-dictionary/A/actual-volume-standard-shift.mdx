---
title: Actual Volume - Standard Shift
description: The Data Dictionary entry for the Actual Volume - Standard Shift entity
slug: /data-dictionary/actual-volume-standard-shift
---

import entities from "@site/src/data/entities.json";

# Actual Volume - Standard Shift

{Array.isArray(entities.actualVolumeStandardShift.description) ? (
entities.actualVolumeStandardShift.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.actualVolumeStandardShift.description.split('\n').map((paragraph,
index) => (<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualVolumeStandardShift.supportedTypes}
/>

## Data Granularity

{entities.actualVolumeStandardShift.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualVolumeStandardShift.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.actualVolumeStandardShift.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.actualVolumeStandardShift.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.actualVolumeStandardShift.relationship}
  simple={true}
/>

{entities.actualVolumeStandardShift.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualVolumeStandardShift.additionalInfo) ? (
      entities.actualVolumeStandardShift.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualVolumeStandardShift.additionalInfo}</p>
    )}

  </>
)}
