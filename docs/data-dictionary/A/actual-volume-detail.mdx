---
title: Actual Volume Detail
description: The Data Dictionary entry for the Actual Volume Detail entity
slug: /data-dictionary/actual-volume-detail
---

import entities from "@site/src/data/entities.json";

# Actual Volume Detail

{Array.isArray(entities.actualVolumeDetail.description) ? (
entities.actualVolumeDetail.description.map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
) : (
entities.actualVolumeDetail.description.split('\n').map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualVolumeDetail.supportedTypes}
/>

## Data Granularity

{entities.actualVolumeDetail.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualVolumeDetail.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.actualVolumeDetail.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.actualVolumeDetail.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.actualVolumeDetail.relationship}
  simple={true}
/>

{entities.actualVolumeDetail.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualVolumeDetail.additionalInfo) ? (
      entities.actualVolumeDetail.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualVolumeDetail.additionalInfo}</p>
    )}

  </>
)}
