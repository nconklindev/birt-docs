---
title: Activity Transactions
description: The Data Dictionary entry for the Activity Transactions entity
slug: /data-dictionary/activity-transactions
---

import entities from "@site/src/data/entities.json";

# Activity Transactions

{Array.isArray(entities.activityTransactions.description) ? (
entities.activityTransactions.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityTransactions.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.activityTransactions.supportedTypes}
/>

## Data Granularity

{entities.activityTransactions.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityTransactions.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.activityTransactions.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.activityTransactions.coreEntity} />

## Relationship

<RelationshipTable relationship={entities.activityTransactions.relationship} />

{entities.activityTransactions.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activityTransactions.additionalInfo) ? (
      entities.activityTransactions.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityTransactions.additionalInfo}</p>
    )}

  </>
)}
