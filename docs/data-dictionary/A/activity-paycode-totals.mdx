---
title: Activity Paycode Totals
description: The Data Dictionary entry for the Activity Paycode Totals entity
slug: /data-dictionary/activity-paycode-totals
---

import entities from "@site/src/data/entities.json";

# Activity Paycode Totals

{Array.isArray(entities.activityPaycodeTotals.description) ? (
entities.activityPaycodeTotals.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityPaycodeTotals.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.activityPaycodeTotals.supportedTypes}
/>

## Data Granularity

{entities.activityPaycodeTotals.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityPaycodeTotals.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.activityPaycodeTotals.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.activityPaycodeTotals.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.activityPaycodeTotals.relationship}
  simple={true}
/>

{entities.activityPaycodeTotals.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activityPaycodeTotals.additionalInfo) ? (
      entities.activityPaycodeTotals.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityPaycodeTotals.additionalInfo}</p>
    )}

  </>
)}
