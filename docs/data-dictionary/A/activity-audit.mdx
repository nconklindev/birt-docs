---
title: Activity Audit
description: The Data Dictionary entry for the Activity Audit entity
slug: /data-dictionary/activity-audit
---

import entities from "@site/src/data/entities.json";

# Activity Audit

{Array.isArray(entities.activityAudit.description) ? (
entities.activityAudit.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activityAudit.description}</p>
)}

## Supported Types

<SupportedTypesTable supportedTypes={entities.activityAudit.supportedTypes} />

## Data Granularity

{entities.activityAudit.dataGranularity}

## Columns

<ColumnsTable columns={entities.activityAudit.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.activityAudit.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.activityAudit.coreEntity} />

## Relationship

<RelationshipTable relationship={entities.activityAudit.relationship} />

{entities.activityAudit.additionalInfo && (

<> ## Additional Information

    {Array.isArray(entities.activityAudit.additionalInfo) ? (
      entities.activityAudit.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activityAudit.additionalInfo}</p>
    )}

</>
)}
