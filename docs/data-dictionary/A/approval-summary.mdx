---
title: Approval Summary
description: The Data Dictionary entry for the Approval Summary entity
slug: /data-dictionary/approval-summary
---

import entities from "@site/src/data/entities.json";

# Approval Summary

{Array.isArray(entities.approvalsReviewers.description) ? (
entities.approvalsReviewers.description.map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
) : (
entities.approvalsReviewers.description.split('\n').map((paragraph, index) => (
<p key={index}>{paragraph}</p>
))
)}

## Supported Types

<SupportedTypesTable supportedTypes={entities.approvalSummary.supportedTypes} />

## Data Granularity

{entities.approvalSummary.dataGranularity}

## Columns

<ColumnsTable columns={entities.approvalSummary.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.approvalSummary.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.approvalSummary.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.approvalSummary.relationship}
  simple={true}
/>

{entities.approvalSummary.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.approvalSummary.additionalInfo) ? (
      entities.approvalSummary.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.approvalSummary.additionalInfo}</p>
    )}

  </>
)}
