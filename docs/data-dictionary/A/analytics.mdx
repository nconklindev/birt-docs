---
title: Analytics
description: The Data Dictionary entry for the Analytics entity
slug: /data-dictionary/analytics
---

import entities from "@site/src/data/entities.json";

# Analytics

{Array.isArray(entities.analytics.description) ? (
entities.analytics.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.analytics.description.split('\n').map((paragraph, index) => (<p
  key={index}
>
  {paragraph}
</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.analytics.supportedTypes} />

## Data Granularity (Employee)

{entities.analytics.dataGranularity.employee}

## Data Granulariy (Business Structure)

{entities.analytics.dataGranularity.businessStructure}

## Columns

<ColumnsTable columns={entities.analytics.columns} />

## Related Entities

### Employee

- [Employee Details](/docs/data-dictionary/employee-details)
- [Sign Off](/docs/data-dictionary/sign-off)

### Business Structure

- [Location](/docs/data-dictionary/location)

## Core Entity (Employee)

<CoreEntityTable coreEntities={entities.analytics.coreEntity.employee} />

## Core Entity (Business Structure)

<CoreEntityTable coreEntities={entities.analytics.coreEntity.orgJob} />

## Relationship

<RelationshipTable
  relationship={entities.analytics.relationship}
  simple={true}
/>

{entities.analytics.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.analytics.additionalInfo) ? (
      entities.analytics.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.analytics.additionalInfo}</p>
    )}

  </>
)}
