---
title: Actual Volume
description: The Data Dictionary entry for the Actual Volume entity
slug: /data-dictionary/actual-volume
---

import entities from "@site/src/data/entities.json";

# Actual Volume

{Array.isArray(entities.actualVolume.description) ? (
entities.actualVolume.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.actualVolume.description.split('\n').map((paragraph, index) =>
(<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.actualVolume.supportedTypes} />

## Data Granularity

{entities.actualVolume.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualVolume.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.actualVolume.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.actualVolume.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.actualVolume.relationship}
  simple={true}
/>

{entities.actualVolume.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualVolume.additionalInfo) ? (
      entities.actualVolume.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualVolume.additionalInfo}</p>
    )}

  </>
)}
