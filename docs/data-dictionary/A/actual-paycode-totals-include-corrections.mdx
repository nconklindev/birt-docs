---
title: Actual Paycode Totals (Include Corrections)
description: The Data Dictionary entry for the Actual Paycode Totals (Include Corrections) entity
slug: /data-dictionary/actual-paycode-totals-include-corrections
---

import entities from "@site/src/data/entities.json";

# Actual Paycode Totals (Include Corrections)

{Array.isArray(entities.actualPaycodeTotalsIncludeCorrections.description) ? (
entities.actualPaycodeTotalsIncludeCorrections.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : ( entities.actualPaycodeTotalsIncludeCorrections.description.split('\n').map((paragraph,
index) => (<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualPaycodeTotalsIncludeCorrections.supportedTypes}
/>

## Data Granularity

{entities.actualPaycodeTotalsIncludeCorrections.dataGranularity}

## Columns

<ColumnsTable
  columns={entities.actualPaycodeTotalsIncludeCorrections.columns}
/>

## Related Entities

<RelatedEntitiesList
  relatedEntities={
    entities.actualPaycodeTotalsIncludeCorrections.relatedEntities
  }
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.actualPaycodeTotalsIncludeCorrections.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.actualPaycodeTotalsIncludeCorrections.relationship}
  simple={true}
/>

{entities.actualPaycodeTotalsIncludeCorrections.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.actualPaycodeTotalsIncludeCorrections.additionalInfo) ? (
      entities.actualPaycodeTotalsIncludeCorrections.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualPaycodeTotalsIncludeCorrections.additionalInfo}</p>
    )}

  </>
)}
