---
title: Audit Forecasting
description: The Data Dictionary entry for the Audit Forecasting entity
slug: /data-dictionary/audit-forecasting
---

import entities from "@site/src/data/entities.json";

# Audit Forecasting

{/* prettier-ignore */}
{Array.isArray(entities.auditForecasting.description) ? (
  entities.auditForecasting.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.auditForecasting.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.auditForecasting.supportedTypes}
/>

## Data Granularity

{entities.auditForecasting.dataGranularity}

## Columns

<ColumnsTable columns={entities.auditForecasting.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.auditForecasting.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.auditForecasting.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.auditForecasting.relationship}
  simple={true}
/>

{entities.auditForecasting.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.auditForecasting.additionalInfo) ? (
      entities.auditForecasting.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.auditForecasting.additionalInfo}</p>
    )}

  </>
)}
