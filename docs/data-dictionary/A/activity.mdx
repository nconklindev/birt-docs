---
title: Activity
description: The Data Dictionary entry for the Activity entity
slug: /data-dictionary/activity
---

import entities from "@site/src/data/entities.json";

# Activity

{Array.isArray(entities.activity.description) ? (
entities.activity.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.activity.description}</p>
)}

## Supported Types

<SupportedTypesTable supportedTypes={entities.activity.supportedTypes} />

## Data Granularity

{entities.activity.dataGranularity}

## Columns

<ColumnsTable columns={entities.activity.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.activity.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.activity.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.activity.relationship}
  simple={true}
/>

{entities.activity.additionalInfo && (

  <>
    ## Additional Information

    {Array.isArray(entities.activity.additionalInfo) ? (
      entities.activity.additionalInformation.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.activity.additionalInfo}</p>
    )}

  </>
)}
