---
title: Actual Paycode Totals (Corrections Only)
description: The Data Dictionary entry for the Actual Paycode Totals (Corrections Only) entity
slug: /data-dictionary/actual-paycode-totals-corrections-only
---

import entities from "@site/src/data/entities.json";

# Actual Paycode Totals (Corrections Only)

{Array.isArray(entities.actualPaycodeTotalsCorrectionsOnly.description) ? (
entities.actualPaycodeTotalsCorrectionsOnly.description.map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) ) : (<p>{entities.actualPaycodeTotalsCorrectionsOnly.description}</p>
)}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.actualPaycodeTotalsCorrectionsOnly.supportedTypes}
/>

## Data Granularity

{entities.actualPaycodeTotalsCorrectionsOnly.dataGranularity}

## Columns

<ColumnsTable columns={entities.actualPaycodeTotalsCorrectionsOnly.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.actualPaycodeTotalsCorrectionsOnly.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.actualPaycodeTotalsCorrectionsOnly.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.actualPaycodeTotalsCorrectionsOnly.relationship}
  simple={true}
/>

{entities.actualPaycodeTotalsCorrectionsOnly.additionalInfo && (

<> <h2>Additional Information</h2>

    {Array.isArray(entities.actualPaycodeTotalsCorrectionsOnly.additionalInfo) ? (
      entities.actualPaycodeTotalsCorrectionsOnly.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.actualPaycodeTotalsCorrectionsOnly.additionalInfo}</p>
    )}

</>
)}
