---
title: Auditor Pattern Strength
description: The Data Dictionary entry for the Auditor Pattern Strength entity
slug: /data-dictionary/auditor-pattern-strength
---

import entities from "@site/src/data/entities.json";

# Auditor Pattern Strength

{/* prettier-ignore */}
{Array.isArray(entities.auditorPatternStrength.description) ? (
  entities.auditorPatternStrength.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.auditorPatternStrength.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.auditorPatternStrength.supportedTypes}
/>

## Data Granularity

{entities.auditorPatternStrength.dataGranularity}

## Columns

<ColumnsTable columns={entities.auditorPatternStrength.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.auditorPatternStrength.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.auditorPatternStrength.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.auditorPatternStrength.relationship}
  simple={true}
/>

{entities.auditorPatternStrength.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.auditorPatternStrength.additionalInfo) ? (
      entities.auditorPatternStrength.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.auditorPatternStrength.additionalInfo}</p>
    )}

  </>
)}
