---
title: Budget Volume Detail - Schedule Zone
description: The Data Dictionary entry for the Budget Volume Detail - Schedule Zone entity
slug: /data-dictionary/budget-volume-detail-schedule-zone
---

import entities from "@site/src/data/entities.json";

# Budget Volume Detail - Schedule Zone

{/* prettier-ignore */}
{Array.isArray(entities.budgetVolumeDetailScheduleZone.description) ? (
  entities.budgetVolumeDetailScheduleZone.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.budgetVolumeDetailScheduleZone.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.budgetVolumeDetailScheduleZone.supportedTypes}
/>

## Data Granularity

{entities.budgetVolumeDetailScheduleZone.dataGranularity}

## Columns

<ColumnsTable columns={entities.budgetVolumeDetailScheduleZone.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.budgetVolumeDetailScheduleZone.relatedEntities}
/>

## Core Entity

<CoreEntityTable
  coreEntities={entities.budgetVolumeDetailScheduleZone.coreEntity}
/>

## Relationship

<RelationshipTable
  relationship={entities.budgetVolumeDetailScheduleZone.relationship}
  simple={true}
/>

{entities.budgetVolumeDetailScheduleZone.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.budgetVolumeDetailScheduleZone.additionalInfo) ? (
      entities.budgetVolumeDetailScheduleZone.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.budgetVolumeDetailScheduleZone.additionalInfo}</p>
    )}

  </>
)}
