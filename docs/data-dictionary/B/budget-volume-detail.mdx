---
title: Budget Volume Detail
description: The Data Dictionary entry for the Budget Volume Detail entity
slug: /data-dictionary/budget-volume-detail
---

import entities from "@site/src/data/entities.json";

# Budget Volume Detail

{/* prettier-ignore */}
{Array.isArray(entities.budgetVolumeDetail.description) ? (
  entities.budgetVolumeDetail.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.budgetVolumeDetail.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.budgetVolumeDetail.supportedTypes}
/>

## Data Granularity

{entities.budgetVolumeDetail.dataGranularity}

## Columns

<ColumnsTable columns={entities.budgetVolumeDetail.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.budgetVolumeDetail.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.budgetVolumeDetail.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.budgetVolumeDetail.relationship}
  simple={true}
/>

{entities.budgetVolumeDetail.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.budgetVolumeDetail.additionalInfo) ? (
      entities.budgetVolumeDetail.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.budgetVolumeDetail.additionalInfo}</p>
    )}

  </>
)}
