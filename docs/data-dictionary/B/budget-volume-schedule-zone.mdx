---
title: Budget Volume - Schedule Zone
description: The Data Dictionary entry for the Budget Volume - Schedule Zone entity
slug: /data-dictionary/budget-volume-schedule-zone
---

import entities from "@site/src/data/entities.json";

# Budget Volume - Schedule Zone

{/* prettier-ignore */}
{Array.isArray(entities.budgetVolumeScheduleZone.description) ? (
  entities.budgetVolumeScheduleZone.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.budgetVolumeScheduleZone.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable
  supportedTypes={entities.budgetVolumeScheduleZone.supportedTypes}
/>

## Data Granularity

{entities.budgetVolumeScheduleZone.dataGranularity}

## Columns

<ColumnsTable columns={entities.budgetVolumeScheduleZone.columns} />

## Related Entities

<RelatedEntitiesList
  relatedEntities={entities.budgetVolumeScheduleZone.relatedEntities}
/>

## Core Entity

<CoreEntityTable coreEntities={entities.budgetVolumeScheduleZone.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.budgetVolumeScheduleZone.relationship}
  simple={true}
/>

{entities.budgetVolumeScheduleZone.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.budgetVolumeScheduleZone.additionalInfo) ? (
      entities.budgetVolumeScheduleZone.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.budgetVolumeScheduleZone.additionalInfo}</p>
    )}

  </>
)}
