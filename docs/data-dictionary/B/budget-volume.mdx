---
title: Budget Volume
description: The Data Dictionary entry for the Budget Volume entity
slug: /data-dictionary/budget-volume
---

import entities from "@site/src/data/entities.json";

# Budget Volume

{/* prettier-ignore */}
{Array.isArray(entities.budgetVolume.description) ? (
  entities.budgetVolume.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.budgetVolume.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.budgetVolume.supportedTypes} />

## Data Granularity

{entities.budgetVolume.dataGranularity}

## Columns

<ColumnsTable columns={entities.budgetVolume.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.budgetVolume.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.budgetVolume.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.budgetVolume.relationship}
  simple={true}
/>

{entities.budgetVolume.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.budgetVolume.additionalInfo) ? (
      entities.budgetVolume.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.budgetVolume.additionalInfo}</p>
    )}

  </>
)}
