---
title: Reviewer List Assignments
description: The Data Dictionary entry for the Reviewer List Assignments entity
slug: /data-dictionary/reviewer-list-assignments
---

import entities from "@site/src/data/entities.json";

# Reviewer List Assignments

{/* prettier-ignore */}
{Array.isArray(entities.reviewerListAssignments.description) ? (
  entities.reviewerListAssignments.description.map((paragraph, index) => (
    <p key={index}>{paragraph}</p>
))
) : (
entities.reviewerListAssignments.description.split('\n').map((paragraph, index) => (

<p key={index}>{paragraph}</p>
)) )}

## Supported Types

<SupportedTypesTable supportedTypes={entities.reviewerListAssignments.supportedTypes} />

## Data Granularity

{entities.reviewerListAssignments.dataGranularity}

## Columns

<ColumnsTable columns={entities.reviewerListAssignments.columns} />

## Related Entities

<RelatedEntitiesList relatedEntities={entities.reviewerListAssignments.relatedEntities} />

## Core Entity

<CoreEntityTable coreEntities={entities.reviewerListAssignments.coreEntity} />

## Relationship

<RelationshipTable
  relationship={entities.reviewerListAssignments.relationship}
  simple={true}
/>

{entities.reviewerListAssignments.additionalInfo && (

  <>
    <h2>Additional Information</h2>

    {Array.isArray(entities.reviewerListAssignments.additionalInfo) ? (
      entities.reviewerListAssignments.additionalInfo.map((info, index) => (
        <p key={index}>{info}</p>
      ))
    ) : (
      <p>{entities.reviewerListAssignments.additionalInfo}</p>
    )}

  </>
)}
